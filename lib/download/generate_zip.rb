class Download::GenerateZip

  def initialize
    puts "initialize generate zip with the path of the zip path"
  end

  def add_file_to_zip(file_path, zipped_file_path)
    puts "adds file to zip (updates it if present) and creates zip if not already present"
  end

  def application_generate_zip(form_id)
    puts "generate zip for single application and add manual attachment to submission model"
  end

  def all_applications_generate_zip(vacancy_id)
    puts "generate zip for all applications and add manual attachment to form model"
  end

end
