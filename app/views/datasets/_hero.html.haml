.hero#data{ :style => "background: url(#{cms_page_content(:background, datasets_page).try(:file).try(:url)}) no-repeat center; background-size: 100%" }
  .heading
    %h1= cms_page_content(:header, datasets_page)
    %h2.shorter= cms_page_content(:subheader, datasets_page)
