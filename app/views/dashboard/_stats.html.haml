%div{:class => "dashboard", "ng-app" => "stats", "ng-controller" => "SapiStatsCtrl"} 

  %ul
    %li{'ng-repeat' => "s in species_cites_eu"} {{s.common_name_en}} ({{s.name}}): {{s.count}}

  %ul
    %li{'ng-repeat' => "s in species_cms"} {{s.common_name_en}} ({{s.name}}): {{s.count}}


  %ul
    %li{'ng-repeat' => "t in trade_exports_top"} {{t.common_name_en}} ({{t.name}}): {{t.count}}

  %ul
    %li{'ng-repeat' => "t in trade_imports_top"} {{t.common_name_en}} ({{t.name}}): {{t.count}}


  